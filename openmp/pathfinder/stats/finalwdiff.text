 *wall; *data;int **wall;    wall    data    wall = new int *[rows];    for (int n = 0; n < rows; n++)        wall[n] = data + cols * n;            wall[i*rows + j]            wall[i][j] wall[j]; wall[0][j]; wall[i*rows+j]); wall[i][j]);#ifdef OMP_DC    #pragma omp target enter data map(to: wall[:rows][:cols]) wall[:rows*cols],#else#pragma omp target enter data map (to: src[:cols], wall[:rows], dst[:cols])    for (int i = 0; i < rows; i++) {      #pragma omp target enter data map(to: wall[i][:cols])    }#endif wall[(t wall[t 1)*rows+n] 1][n] wall[i]); data[i]);    delete[] data;
